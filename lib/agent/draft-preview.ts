/**
 * A generated post draft, produced in "preview" mode (NOT yet persisted to the
 * Posts library). The user reviews this and then chooses to save it as a draft.
 *
 * Pure types only — safe to import from client components, server actions, and
 * server-only lib code alike.
 */
export interface DraftPreview {
  body: string
  hook: string | null
  format: string | null
  archetype: string | null
  skill_slug: string | null
}
